{
  "name": "jarvis-memory-bridge",
  "version": "1.0.0",
  "description": "Persistent short-/long-term memory (SQLite) for Jarvis's Node peripherals, with a token-gated WebSocket and HTTP API.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "sqlite3": "^5.1.7",
    "sqlite": "^5.1.1",
    "express": "^4.19.2",
    "ws": "^8.18.0"
  }
}
